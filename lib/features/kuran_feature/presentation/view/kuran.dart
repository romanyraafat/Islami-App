import 'package:flutter/material.dart';

class Kuran extends StatelessWidget {
  const Kuran({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Kuran"),
      ),
    );
  }
}